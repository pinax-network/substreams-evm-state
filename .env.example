# Copy to .env and fill in. Never commit .env.
# Substreams auth (either works for the substreams CLI and substreams-sink-sql)
SUBSTREAMS_API_KEY=
# SUBSTREAMS_API_TOKEN=
# JSON-RPC used by scripts/verify_rpc.py
RPC_URL=https://bsc.rpc.pinax.network
RPC_API_KEY=
